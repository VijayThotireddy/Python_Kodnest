PYTHON INTRODUCTION:

Topics:
1.Intro to Python
2.Getting started code with Python
3.Python syntax and data types and strings
4.User Input & Type casting
5.Operators
6.Functions
7.control statements



1.INTRO TO PYTHON

--- GUDO VAN ROMAN-----
Python -> interpter lang - Procedural -> c - Scripting -> JS - object orinetd -> JAVA
->Python is an interpreted, high-level, general-purpose programming language.
->Python is multi-paradigm : Its supports object oriendtd, procedural, Functional programming.

Feautres:
1.Cross platform - Platform Independnt
2.Dynamically type - No needed to specify data type [ Interview qn ]
3.Simple and Easy
4.Interpreted lang - Line by Line
5.Library - Inbuilt lib
6.Friendly - Large community


2.GETTING STARTED CODE WITH PYTHON

Print("Hello,World") - It executes line by line , whereas complier complied all code and exceute and give err.

-> Multi - line
    print("hello")
    print("world)

->Printing on same linne
    print("Hello", end=" ")
    print("world)

->Comments
  1.Single line - #
  2.Multi-line -  ''' ******* '''

->Combining print() and comments

  #printing name
  name="vijay"
  print("Hello", + name + " hai")

->Tab \t
  print("hello\tworld")

->NEW LINE \n
  print("hello world\n")

->Separate \s
    print("30","12","2004",sep="/") -> 30/12/2004

#PYTHON CODE FLOW OF EXECUTION:
  1.source code -.py file
  2.Python Interpreter reads code
  3.Byte compalaition - changes py code into byte code
  4.PVM executes Bytecode - changes to bytecode to machine code
  5.Machine code processing - cpu performs machine code and taskes action
  6.Output Displayed


eg:
a="10"
b=9
print(a+b) - Type err : str +int can't cancaet
print(a,b) - 10,9
print(type(a)) - <class<str>>


3.PYTHON SYNTAX AND DATA TYPES:

Variables:
    -> Is a container which stores data , label given to data.
    eg: a =10
        a-> is a variable or label
        10-> is data in java , in python its a object.

    print(type(a)) -> <class<int>> 
    sys.out.print(type(a)) -> Number

""Variable is name or label given to a memory location in ordder to store data""
""everything py is treated as object""

Rules:
- Letters(a-z, A-Z), underscore, number(0-9)
- Shouldn't strat with number.
- space is not allowed
- case sensitive
- Keywords can't be used
- Complete varible name - camel[teacherSalary] , pasacal [TeacherSalary], snake[teacher_salary].


Data Types:
    - whst kind of data is stored in a variable.
    TYpes:
                BASIC                        ADVANCED
        1.int -3                               1.List - []
        2.float- 2.3                           2.Tuple - ()
        3.bool  -True or False                 3.Set - {}
        4.string  - "vijay"                    4.Dictionary -{}
        5.complex a+bj


Strings:
    - strings are objects , enclosed in double qoutes,sequence of charactres.
    - str = "hello" or 'hello' or '''hello''' or ""hello""
    - multi line string - use ''' ''' or """ """
    - inline string - use '' or " "
    Ex:
        print('I' am gamana') - invalid
        print("I'am gamana") - valid

        print("I'am gamana from "bangalore"") -invalid
        print('''I'am gamana from "bangalore"''') - invalid
        print("I'am gamana form\"bangalore\"") - valid

More About Strings:
    1.String Slicing
        - excrating some part of a string.
        - Index starts from 0 , end index end-1 or len-1.or n-1 (where n is length of string).
        eg: s="kodnest"
            print(s) #kodnest
            print(len(S))
            print(s(start:stop:step))
            print(s[5])
            print(s[2:5])
            print(s[:4])
            print(s[2:])
            print(s[1:5:2])
            print(s[::2])
            print(s[1::])

        Negative indexing : from right to left : -1,-2,-3,-4...
            print(s[-2])
            print(s[-7:-2])
            print(s[:-1])
            print(s[-5:-1])
            print(s[::-1])
            print(s[-6::-2])
            print(s[-3:-8:-2])

    2.Inbuilt methods:
        Upper , Lower, Capitalize, Title, Strip, Replace, Split, Join, Find, Count, Startswith, endswith, isalpha, isalnum

        EX: 
            S1 = "kodnest"
            s2 = "KODNEST"
            S3="  python  "
            s4="hello world"
            s5="python is awesome"
            s6="banaana"
            s7="12345"
            s8="python123"
            
            L1=["I","am","Gamana"]
            
            print(s1.upper()) - KODNEST
            print(s2.lower()) - kodnest
            print(s4.captialize()) - Hello world (only 1st letter capital)
            print(s5.title()) - Python Is Awesome 
            print(s3.strip()) - removing trail, leading spoaces - python
            print(s4.replace("world","python") - hello python
            print(s5.split(","))  - ["python", "is", "awesome"] - split into list (words).
            print("-".join(["python","is","fun"]) - Python-is-fun
            print("#".join(l1)) - I#am#gamana
            print(s1.find("n")) - gives index position - 3[-1]
            print(s6.count("a")) - 3
            print("n" in s1) - True
            print(s1.stratswith("ko")) - True
            print(s1.endswith("est")) - true
            print(s7.isdigit()) - True
            print(s1.isalpha()) - true
            print(s1.conact("hai")) -err becoz there is no method concat in python use " + "
            print(s1+"hai")
            print(id(S1) - gets u the address of the value.
            print(s8.isalnum()) - true

            S1="python"
            s2="python"
            print(s1 is s2) - is is membership operator (checks for address) ans: True





4.User Input and Type Casting:

    input("entre ur name")
    int(input())
    str(input())
    float(input())
    bool(input())

    -> input() : default it accepts as string.

    ->TYpe casting: converting one typw of data to another.



5.OPERATORS:

        a + b
        a,b -> operands
         +  -> operator

        TYpes :
        1.Arthiematic
        2.comparison
        3.Logical
        4.Assignment
        5.Bitwise
        6.Identity
        7.Membership

        1.Arithematic : +, -, *, %, //, **, /
                / -> x=15, y=4 ans: 3.75
                //-> ans:3 - round value
                % -> ans:3 -> remainder

        2.Comparision : > ,< ,>=, <=, ==, !=

        3.Logical : And, Or, Not
                And -> a=(5>3) and b=(4<6) - true
                Or -> a=(5<3) or b=(4<6) -true
                Not -> a=not(5<6) - false

        4.Assignment: =,+=, -=, *=, /=, %=
            a=5
            a+=3 ->8
            a-=3 ->2
            a*=3 ->15
            a%=2 ->1

        5.Bitwise : &, |, >>, <<, ~, ^
            & -> a=5 & b=3 -> a is 1
            | -> a=5 | b=3 -> a is 7
            ^ -> a=5^3 -> a is 6
            ~ -> a=~5 -> a is -6
            a=5 << 1 -> a is 10
            a=5 >> 1 -> a is 2

        6.Identity:
            x=["apple","mango"], Y=["apple","mango"], z=x
            print(x is z) -> true
            print(x==y) -> true
            print(x is y) -> false

        7.membership: in, not in

            fruits=["apple","mango","gra[pes]
            print("apple" in fruits) -> true



6.FUNCTIONS:

           intervew : STAR - situtation -  task - action - result

            def -> define
            syntax :
                    def function_name(params ->optional):
                        fun_body
                        return -> optional

                    function_name(argument) ->fun call

            eg: def greet():
                    print("good morning")
                greet()

            a>parameters and arguments
                def greet(name):
                    pritn(f"good morning {name}")
                greet("anu") 

             b>default paarmeters
                def greet(name="anu"):

            Types of functions:
            1.NO Arguments and No return value
                def sqr():
                    num=1
                    print(num*num)
                sqr()
            
            2.No argument and return value
                def sqr():
                    num=2
                    return num*num
                n=sqr()
                print(n)
            
            3.Argument and no return
                def sqr(num):
                    print(num*num)
                n=sqr(3)
                print(n)
            
            4.Argument and return
                dfe sqr(num):
                    return num*num
                n=sqr(23)
                print(n)



7.CONTROL CONSTRUCTS:

    1.Conditional - > if, else, elif , nested, switch(match in py)
    2.Loops -> for, while
    3.Special controls -> break, contiune, pass, return
            
                       
                            
                        
                            
